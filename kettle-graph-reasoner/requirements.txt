# KGR pinned dependencies — the environment the frozen v1.0 checkpoint and
# the h32 chain were validated on (Python 3.11, Windows; Colab-compatible).
# These are EXACT pins for reproducible installs. If you need newer
# versions, change the pin deliberately and re-run the verification chain
# (tests/ + verify.py P0-P2) before trusting any eval number.
#
# Torch note: hyperbolic stability + the locked baseline rely on
# torch >= 2.4 (nn.RMSNorm fallback in src/modelsv3/query_encoder.py).
# Bumping torch can invalidate the checkpoint's expected ops; verify a
# Stage-A trajectory matches the locked recipe before declaring a new pin.
#
# scripts/requirements.txt is the SEPARATE, intentionally-lean env for the
# graph-diagnostics tooling only (no torch). This file is the full
# research + serving env; install this one unless you know you only need
# the diagnostics CLI.

# Core numerics / graphs
numpy==2.2.6
scipy==1.15.3
networkx==3.6.1

# ML stack
torch==2.7.1
geoopt==0.5.1

# Neo4j / config (exporter, diagnostics, serving layer)
neo4j==6.1.0
python-dotenv==1.0.0
PyYAML==6.0

# Serving layer (src/service/explorer_app.py + context service HTTP API)
flask==3.1.3
requests==2.32.3

# Tooling
rapidfuzz==3.14.3
pandas==2.3.3
pyarrow==20.0.0
pytest==9.0.3

# Optional Colab-only — uncomment for cloud runs that report to wandb.
# wandb>=0.16
